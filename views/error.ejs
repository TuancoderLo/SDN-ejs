<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Perfume SDN</title>
    <link href="https://cdn.jsdelivr.net/npm/daisyui@5" rel="stylesheet" type="text/css" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <style>
        .luxury-gradient {
            background: linear-gradient(135deg, #1e3a8a 0%, #1e40af 25%, #3b82f6 50%, #06b6d4 75%, #0891b2 100%);
        }
        .premium-blue {
            background: linear-gradient(135deg, #1e40af 0%, #3b82f6 100%);
        }
        .ocean-gradient {
            background: linear-gradient(135deg, #0ea5e9 0%, #06b6d4 50%, #0891b2 100%);
        }
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        @keyframes float {
            0%, 100% {
                transform: translateY(0px);
            }
            50% {
                transform: translateY(-10px);
            }
        }
        @keyframes pulse-glow {
            0%, 100% {
                box-shadow: 0 0 20px rgba(239, 68, 68, 0.3);
            }
            50% {
                box-shadow: 0 0 30px rgba(239, 68, 68, 0.6);
            }
        }
        .animate-fadeInUp {
            animation: fadeInUp 0.6s ease-out;
        }
        .animate-float {
            animation: float 3s ease-in-out infinite;
        }
        .animate-pulse-glow {
            animation: pulse-glow 2s ease-in-out infinite;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-red-50 via-white to-orange-50 min-h-screen">
    <%- include('partials/navbar') %>

    <!-- Main Content -->
    <main class="min-h-screen flex items-center justify-center py-12">
        <div class="container mx-auto px-4">
            <div class="max-w-2xl mx-auto">
                <!-- Error Card -->
                <div class="card bg-white/95 backdrop-blur-md shadow-2xl border border-red-200/50 rounded-2xl animate-fadeInUp">
                    <div class="card-body p-8 text-center">
                        <!-- Error Icon with Animation -->
                        <div class="mb-8">
                            <div class="inline-flex items-center justify-center w-24 h-24 bg-red-100 rounded-full mb-4 shadow-lg animate-float animate-pulse-glow relative">
                                <i class="fas fa-exclamation-triangle text-4xl text-red-600 animate-float"></i>
                                <div class="absolute inset-0 rounded-full bg-red-200 opacity-50 animate-ping"></div>
                            </div>
                        </div>

                        <!-- Error Message -->
                        <h1 class="text-4xl font-bold text-gray-800 mb-4">
                            <span class="bg-gradient-to-r from-red-600 to-orange-600 bg-clip-text text-transparent">Oops!</span> Something went wrong
                        </h1>
                        
                        <p class="text-lg text-gray-600 mb-8">
                            We encountered an unexpected error. Don't worry, our team has been notified.
                        </p>
                        
                        <div class="space-y-4 mb-8">
                            <div class="alert alert-error shadow-lg">
                                <i class="fas fa-bug text-lg"></i>
                                <span class="font-semibold"><%= message %></span>
                            </div>
                            
                            <% if (typeof error !== 'undefined' && error.status) { %>
                            <div class="alert alert-warning shadow-lg">
                                <i class="fas fa-code text-lg"></i>
                                <span>Error Code: <strong><%= error.status %></strong></span>
                            </div>
                            <% } %>
                        </div>

                        <!-- Error Details (Development only) -->
                        <% if (typeof error !== 'undefined' && error.stack && process.env.NODE_ENV === 'development') { %>
                        <div class="collapse collapse-arrow bg-base-200 mb-6">
                            <input type="checkbox" />
                            <div class="collapse-title text-lg font-medium">
                                <i class="fas fa-code mr-2"></i>
                                Technical Details (Development)
                            </div>
                            <div class="collapse-content">
                                <pre class="text-sm text-left bg-base-300 p-4 rounded-lg overflow-auto max-h-60"><%= error.stack %></pre>
                            </div>
                        </div>
                        <% } %>

                        <!-- Action Buttons -->
                        <div class="flex flex-col sm:flex-row gap-4 justify-center mb-8">
                            <button onclick="history.back()" class="btn btn-outline btn-lg hover:bg-gray-100 transition-all duration-300">
                                <i class="fas fa-arrow-left mr-2"></i>
                                Go Back
                            </button>
                            <a href="/" class="btn btn-primary btn-lg premium-blue hover:ocean-gradient text-white transition-all duration-300 shadow-lg">
                                <i class="fas fa-home mr-2"></i>
                                Home Page
                            </a>
                        </div>

                        <!-- Help Section -->
                        <div class="bg-gradient-to-r from-blue-50 to-cyan-50 rounded-xl p-6 border border-blue-200/50">
                            <div class="flex items-center justify-center mb-4">
                                <div class="w-12 h-12 bg-blue-100 rounded-full flex items-center justify-center">
                                    <i class="fas fa-life-ring text-blue-600 text-xl"></i>
                                </div>
                            </div>
                            <h3 class="text-lg font-semibold text-gray-800 mb-2">Need Help?</h3>
                            <p class="text-gray-600 text-sm mb-4">
                                If this problem persists, please contact our support team or try refreshing the page.
                            </p>
                            <div class="flex justify-center gap-4">
                                <button onclick="location.reload()" class="btn btn-sm btn-outline btn-primary">
                                    <i class="fas fa-sync-alt mr-2"></i>
                                    Refresh Page
                                </button>
                                <a href="mailto:support@perfumesdn.com" class="btn btn-sm btn-outline btn-secondary">
                                    <i class="fas fa-envelope mr-2"></i>
                                    Contact Support
                                </a>
                            </div>
                        </div>

                        <!-- Footer -->
                        <div class="mt-8 text-gray-500 text-sm">
                            <p>
                                <i class="fas fa-shield-alt mr-2"></i>
                                Your data is safe and secure
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Background Decorative Elements -->
    <div class="fixed inset-0 -z-10 overflow-hidden pointer-events-none">
        <div class="absolute top-20 left-20 w-32 h-32 bg-red-200/20 rounded-full animate-float"></div>
        <div class="absolute top-40 right-20 w-24 h-24 bg-orange-200/20 rounded-full animate-float" style="animation-delay: 1s;"></div>
        <div class="absolute bottom-20 left-1/4 w-20 h-20 bg-yellow-200/20 rounded-full animate-float" style="animation-delay: 2s;"></div>
        <div class="absolute bottom-40 right-1/3 w-28 h-28 bg-red-300/20 rounded-full animate-float" style="animation-delay: 0.5s;"></div>
    </div>

    <%- include('partials/footer') %>
    
    <script src="/javascripts/app.js"></script>
</body>
</html>